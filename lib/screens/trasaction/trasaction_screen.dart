import'package:flutter/material.dart';
class TrasactionScreen extends StatelessWidget {
  const TrasactionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Trasaction')),
    );
  }
}